# Task
- Answer the following query about workout history for user {user_id}:
{user_query}

Use the query_workouts tool to retrieve the relevant workout data, then provide a clear and helpful response.

